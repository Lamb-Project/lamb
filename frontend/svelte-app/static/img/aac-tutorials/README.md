# AAC UI tutorial screenshots

Real English Creator UI screenshots captured on the dev line on 13 September 2026. Accounts and course content are synthetic. Orange outlines highlight existing controls; screenshots do not depict an AAC-controlled file picker. The operating system file picker is opened by the user and varies by platform.

These assets accompany `backend/lamb/aac/docs/ui-*.md` and are served at `/img/aac-tutorials/`. The interface still displays its older v0.6 header; these captures document the dev interface used for the 0.7 tutorial work. Available models, plugins, names and identifiers are examples, not defaults to copy.

`kb-files.png` intentionally shows processing, not completion. `kb-query.png` shows an actual query retrieving the uploaded synthetic source. Configuration screenshots show forms and controls, not proof that every depicted mode was saved and tested.

When the UI changes, refresh the affected screenshot and its written steps together. Retain descriptive alt text and the full-size image link. Do not include credentials or real learner data in replacement captures.
